# Required for M3+ (scenario generation and LLM-as-judge).
# Get your key at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-…

# Path to the SQLite database (default: local.db relative to cwd)
DATABASE_PATH=../../local.db
